# AnalyzePDF 智能助手工作规则

开始工作前依次阅读：

1. `docs/HANDOFF.md`：当前唯一任务、真实失败和验收标准；
2. `README.md`：仓库职责、入口和输出；
3. 与当前任务直接相关的代码和测试。

## 新任务启动闸门

- 新任务必须从本仓库根目录启动，先读取本文件和 `docs/HANDOFF.md`。
- 第一次回复只用普通中文复述：当前唯一任务、为什么先做、已经证实的失败、准备查看哪些位置和停止线。
- 用户确认方向后才能修改代码。若仓库状态与交接文档不一致，先报告差异。

## 沟通与完工报告

- 尽量使用中文；英文名词后补中文解释。
- 用户没有听懂当前在做什么时，立即暂停实现，先解释清楚。
- 调用工具前说明本轮目标、可见交付物和停止线。
- 每次完工必须报告：修改文件及理由、实际命令、真实退出码、产物位置、仍未完成的能力、下一步理由。
- 不得把单元测试通过写成真实 PDF 端到端通过，也不得把代码存在写成产品能力完成。

## 当前主线纪律

- 当前只修复 `docs/HANDOFF.md` 中的一个真实失败，不跑批量语料，不增加解析器，不建设评测平台。
- AnalyzePDF 只负责 PDF 解析、OCR（图片文字识别）、质量判断、路线选择、失败降级和统一输出。
- 权限、知识抽取、清洗后的结构发现、检索和问答不在本仓库实现。
- DocumentNormalization 的结构化契约和真实 PDF 主流程没有验收前，不得推进 EmergentKB。
- 不得通过单纯放宽阈值、吞掉错误或强制返回成功来伪造通过。

## 额度与分工

- 主智能助手负责架构判断、关键逻辑、真实核验和是否过闸门。
- Luna 子智能体只做范围明确的机械任务；Grok 只接受用户转交的下载、长命令和批量执行任务。
- 子智能体或外部执行者不得扩大范围、决定架构、提交或推送；主智能助手必须复核差异和真实产物。
- 不通过 Cursor CLI（命令行工具）调用昂贵模型。外部付费或稀缺模型必须事先说明目的、次数、预计费用和停止条件，并由用户批准。
- 长日志写入文件并汇报关键证据，不重复运行已经有完整可核验产物的重任务。

## 隐私和版本控制

- 不提交 PDF 原件、解析正文、模型缓存、密钥、本机绝对路径、虚拟环境或本地输出。
- 不擅自覆盖用户改动，不执行破坏性 Git 命令。
- 未经用户明确要求，不提交、不推送。
