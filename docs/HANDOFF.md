# AnalyzePDF 交接

> 更新时间：2026-08-24
> 状态：普通中文电子 PDF 主路线和 PDF 旁证已完成真实运行，当前不是主线开发仓库
> 仓库：AnalyzePDF

## 当前已经真实具备

- 普通中文电子 PDF 只有可清洗空格警告时，保留 Docling（文档解析工具）结果，不再无意义切换 MinerU（复杂文档解析工具）；
- 发布统一 `ParsedDocument`（解析文档），包含正文、页、元素、表格、图片、附件、坐标和质量状态；
- 发布逐页 PDF 原生文字、带坐标的英文 `Table/Figure` 标签和原页图片旁证；
- 将 PDF 坐标转换为 `0～1` 的格式中立位置；
- 比较主解析正文和 PDF 原生文字，为下游提供解析证据建议；
- 当前自动测试为 `82 passed`，软件包构建成功。

## 真实运行证据

真实 `XZFG-0200.pdf` 已通过统一入口运行：

- 进程退出码 `0`；
- 选中 Docling；
- 发布 18 页原生文字、18 页原页图片和 214 个统一解析元素；
- 中文法规没有英文 `Table/Figure` 标签，因此标签数为 `0`，不代表标签提取失败；
- DocumentNormalization 已独立复核旁证的文档身份、相对路径和哈希。

本地结果：`output/responsibility-split-xzfg0200-v3/`。本地真实正文和产物不提交 Git。

## 仍未完成

- 扫描件、复杂损坏 PDF、部分成功和 MinerU 备用路线的代表性真实验收；
- 多栏和复杂版面是否被主解析器正确重建的自动回归；
- 中文表题、图题的精确标签旁证；
- 自动运行真实 Docling 的端到端回归测试。

## 当前主线与停止线

当前主线在 DocumentNormalization：先定义能承载结构和证据链的下一版 `CleanDocument`（最终清洗文档）契约。

AnalyzePDF 只回答“PDF 中解析出了什么”，不在这里：

- 清洗正文或删除页面装饰；
- 裁决段落边界和阅读顺序；
- 重建跨页逻辑表格、图片和图注关系；
- 实现知识抽取、权限、检索或问答；
- 增加新解析器、批量语料或模型评审设施。

未经用户明确要求，不提交、不推送。新任务开始时必须重新核对 Git 状态；本文记录不能替代现场检查。
